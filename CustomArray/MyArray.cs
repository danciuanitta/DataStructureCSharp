namespace CustomArray
{
    public class MyArray
    {
        //this should be unsafe code given it handles bits and bit moves
        //it keeps a pointer to the address of the first location of array (v[0]) and from there it is easy to retrieve calculating the bits using any index O(1)
        //can't exceed capacity, as opposed to lists
        //adding at the end, in the middle or the begining
        //search
        //remove
        //show how it is done in c#
        //show indexers
    }
}
